<Project>
  <PropertyGroup>
    <!-- Versionierung: MinVer holt die Version aus Git-Tags (z. B. 0.1.0) -->
    <!-- MinVer-Settings -->
    <MinVerTagPrefix></MinVerTagPrefix>
    <MinVerMajorMinor>0.1</MinVerMajorMinor>
    <!-- Pre-Release-Bezeichner für Commits nach dem letzten Tag, z. B. 0.1.1-alpha.0.5 -->
    <MinVerDefaultPreReleaseIdentifiers>alpha.0</MinVerDefaultPreReleaseIdentifiers>

    <!-- Project Metadata -->
    <Company>Andreas Huelsmann</Company>
    <Authors>Andreas Huelsmann</Authors>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <Copyright>Copyright © $([System.DateTime]::Now.Year) Andreas Huelsmann</Copyright>
    <!-- Repository Information -->
    <RepositoryType>git</RepositoryType>
    <RepositoryUrl>https://dev.azure.com/ahuelsmann/MOBAflow/_git/MOBAflow</RepositoryUrl>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <PlatformTarget Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'">x64</PlatformTarget>
    <!-- Any CPU maps to mismatched bin/obj paths for WinUI + MOBAflow dependencies (WMC1006). -->
    <Platform Condition="'$(OS)' == 'Windows_NT' and ('$(Platform)' == '' or '$(Platform)' == 'AnyCPU')">x64</Platform>

    <!-- Keep normal build loops fast; Release remains strict below. -->
    <RunAnalyzersDuringBuild>false</RunAnalyzersDuringBuild>
    <RunAnalyzersDuringLiveAnalysis>true</RunAnalyzersDuringLiveAnalysis>
    <GenerateDocumentationFile Condition="'$(Configuration)' != 'Release'">false</GenerateDocumentationFile>
    <UseSharedCompilation>true</UseSharedCompilation>
    <!-- Reference assemblies reduce bin-folder DLL locking during parallel solution builds (MSB3026/MSB3061). -->
    <ProduceReferenceAssembly>true</ProduceReferenceAssembly>
    <Deterministic>false</Deterministic>
    <Deterministic Condition="'$(Configuration)' == 'Release'">true</Deterministic>

    <!-- Code analysis and warnings policy -->
    <AnalysisMode Condition="'$(Configuration)' == 'Release'">All</AnalysisMode>
    <EnforceCodeStyleInBuild Condition="'$(Configuration)' == 'Release'">true</EnforceCodeStyleInBuild>
    <TreatWarningsAsErrors Condition="'$(Configuration)' == 'Release'">true</TreatWarningsAsErrors>
  </PropertyGroup>

  <!-- MinVer als Build-Only-Dependency für alle Projekte -->
  <ItemGroup>
    <PackageReference Include="MinVer" PrivateAssets="all" />
  </ItemGroup>


  <!-- Debug Configuration -->
  <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
    <Optimize>false</Optimize>
    <DebugType>portable</DebugType>
  </PropertyGroup>

  <!-- FastDebug Configuration - Maximum build speed for daily development -->
  <PropertyGroup Condition=" '$(Configuration)' == 'FastDebug' ">
    <Optimize>false</Optimize>
    <DebugType>embedded</DebugType>
    <DebugSymbols>true</DebugSymbols>
    <RunAnalyzersDuringLiveAnalysis>false</RunAnalyzersDuringLiveAnalysis>
    <DefineConstants>$(DefineConstants);DEBUG;TRACE</DefineConstants>
  </PropertyGroup>

  <!-- Release Configuration -->
  <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
    <Optimize>true</Optimize>
    <DebugType>none</DebugType>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <!-- Legacy/public surface not fully documented; keep Release strict without blocking the build. -->
    <NoWarn>$(NoWarn);CS1591;CS1573</NoWarn>
  </PropertyGroup>
</Project>
