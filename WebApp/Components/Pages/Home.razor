@page "/home"
@inject NavigationManager Navigation

<PageTitle>MOBAdash</PageTitle>

@code {
    protected override void OnInitialized()
    {
        // Redirect to dashboard page on startup
        Navigation.NavigateTo("/dashboard", replace: true);
    }
}