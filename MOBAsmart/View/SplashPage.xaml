<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Moba.MAUI.View.SplashPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="{DynamicResource SplashBackground}"
    Shell.NavBarIsVisible="False">

    <Grid HorizontalOptions="Fill" VerticalOptions="Fill">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Brand splash: fixed dark canvas for logo contrast in both app themes  -->
        <Image
            Grid.Row="1"
            Aspect="AspectFit"
            HeightRequest="200"
            HorizontalOptions="Center"
            Source="splash_background.png"
            VerticalOptions="End" />

        <Label
            Grid.Row="2"
            Margin="0,24,0,0"
            FontAttributes="Bold"
            FontSize="32"
            HorizontalOptions="Center"
            Text="MOBAsmart"
            TextColor="{DynamicResource SplashTextPrimary}"
            VerticalOptions="Start" />

        <VerticalStackLayout
            Grid.Row="3"
            Margin="0,0,0,48"
            HorizontalOptions="Center"
            Spacing="12"
            VerticalOptions="End">
            <ActivityIndicator
                HeightRequest="24"
                IsRunning="True"
                WidthRequest="24"
                Color="{DynamicResource SplashTextPrimary}" />
            <Label
                x:Name="StatusLabel"
                FontSize="13"
                HorizontalOptions="Center"
                Text="Starting..."
                TextColor="{DynamicResource SplashTextSecondary}" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
