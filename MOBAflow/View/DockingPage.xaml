<Page
    x:Class="Moba.WinUI.View.DockingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:docking="using:Moba.WinUI.Controls.Docking"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  DockingManager  -->
        <docking:DockingManager
            x:Name="DockManager"
            Grid.Row="1"
            BottomAutoHidePanels="{Binding BottomAutoHidePanels}"
            BottomNode="{Binding BottomNode}"
            BottomPanelHeight="{Binding BottomPanelHeight, Mode=TwoWay}"
            IsBottomPanelVisible="{Binding IsBottomPanelVisible, Mode=TwoWay}"
            IsLeftPanelVisible="{Binding IsLeftPanelVisible, Mode=TwoWay}"
            IsRightPanelVisible="{Binding IsRightPanelVisible, Mode=TwoWay}"
            IsTopPanelVisible="{Binding IsTopPanelVisible, Mode=TwoWay}"
            LeftAutoHidePanels="{Binding LeftAutoHidePanels}"
            LeftNode="{Binding LeftNode}"
            LeftPanelWidth="{Binding LeftPanelWidth, Mode=TwoWay}"
            RightAutoHidePanels="{Binding RightAutoHidePanels}"
            RightNode="{Binding RightNode}"
            RightPanelWidth="{Binding RightPanelWidth, Mode=TwoWay}"
            TopAutoHidePanels="{Binding TopAutoHidePanels}"
            TopNode="{Binding TopNode}"
            TopPanelHeight="{Binding TopPanelHeight, Mode=TwoWay}">

            <!--  Center: Document Tabs  -->
            <docking:DockingManager.DocumentAreaContent>
                <docking:LayoutDocument
                    ActiveDocument="{Binding ActiveDocument, Mode=TwoWay}"
                    AllowFloatingTabs="False"
                    Documents="{Binding OpenDocuments}"
                    EnableTabGrouping="True">

                    <docking:LayoutDocument.TabTemplate>
                        <DataTemplate>
                            <Grid Padding="4,0" ColumnSpacing="6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <FontIcon
                                    Grid.Column="0"
                                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                                    FontSize="14"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Glyph="{Binding IconGlyph}" />

                                <TextBlock
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                    Text="{Binding Title}"
                                    TextTrimming="CharacterEllipsis" />

                                <FontIcon
                                    Grid.Column="2"
                                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                                    FontSize="9"
                                    Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                    Glyph="&#xE718;"
                                    Visibility="{Binding IsPinned, Converter={StaticResource BoolToVisibilityConverter}}" />

                                <TextBlock
                                    Grid.Column="3"
                                    VerticalAlignment="Center"
                                    FontSize="9"
                                    Foreground="{ThemeResource SystemFillColorCautionBrush}"
                                    Text="*"
                                    Visibility="{Binding IsModified, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </Grid>
                        </DataTemplate>
                    </docking:LayoutDocument.TabTemplate>

                    <docking:LayoutDocument.ContentTemplate>
                        <DataTemplate>
                            <ScrollViewer
                                Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                                HorizontalScrollMode="Disabled"
                                VerticalScrollMode="Auto">
                                <ContentPresenter Margin="0" Content="{Binding Content}" />
                            </ScrollViewer>
                        </DataTemplate>
                    </docking:LayoutDocument.ContentTemplate>
                </docking:LayoutDocument>
            </docking:DockingManager.DocumentAreaContent>

            <!--  Status Bar  -->
            <docking:DockingManager.StatusBarContent>
                <Grid
                    Height="24"
                    Padding="8,0"
                    Background="{ThemeResource AccentFillColorDefaultBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        FontSize="11"
                        Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                        Text="Ready" />

                    <StackPanel
                        Grid.Column="2"
                        Orientation="Horizontal"
                        Spacing="16">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="11"
                            Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                            Text="Ln 42" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="11"
                            Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                            Text="Col 18" />
                    </StackPanel>

                    <TextBlock
                        Grid.Column="3"
                        Margin="16,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="11"
                        Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                        Text="UTF-8" />

                    <TextBlock
                        Grid.Column="4"
                        Margin="16,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="11"
                        Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                        Text="CRLF" />
                </Grid>
            </docking:DockingManager.StatusBarContent>
        </docking:DockingManager>
    </Grid>
</Page>