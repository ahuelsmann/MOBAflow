<Page
    x:Class="Moba.WinUI.View.TrackPlanPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:controls="using:Moba.WinUI.Controls"
    xmlns:controls1="using:CommunityToolkit.WinUI.Controls">
    <Page.KeyboardAccelerators>
        <KeyboardAccelerator Key="Delete" Invoked="OnDeleteAccelerator" />
        <KeyboardAccelerator Key="Back" Invoked="OnDeleteAccelerator" />
        <KeyboardAccelerator
            Key="Z"
            Invoked="OnUndoAccelerator"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="Y"
            Invoked="OnRedoAccelerator"
            Modifiers="Control" />
    </Page.KeyboardAccelerators>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!--  Toolbar  -->
        <CommandBar
            Grid.Row="0"
            DefaultLabelPosition="Right"
            OverflowButtonVisibility="Auto">
            <AppBarButton
                x:Name="UndoButton"
                Icon="Undo"
                Label="Undo" />
            <AppBarButton
                x:Name="RedoButton"
                Icon="Redo"
                Label="Redo" />
            <AppBarElementContainer VerticalAlignment="Center">
                <DropDownButton
                    x:Name="ExportDropDownButton"
                    Padding="8,4"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderThickness="0"
                    ToolTipService.ToolTip="Export options">
                    <DropDownButton.Content>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <SymbolIcon Symbol="OpenFile" />
                            <TextBlock Text="Export" />
                        </StackPanel>
                    </DropDownButton.Content>
                    <DropDownButton.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem
                                x:Name="ExportSvgInBrowserMenuItem"
                                Text="SVG in Browser"
                                ToolTipService.ToolTip="Exports the current track plan as SVG and opens it in the browser">
                                <MenuFlyoutItem.Icon>
                                    <SymbolIcon Symbol="Globe" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
            </AppBarElementContainer>
            <AppBarButton
                x:Name="ValidateButton"
                Icon="Accept"
                Label="Validate" />
            <AppBarButton
                x:Name="FitButton"
                Icon="ZoomIn"
                Label="Fit" />
            <AppBarButton
                x:Name="ResetZoomButton"
                Icon="Zoom"
                Label="100%" />
            <AppBarSeparator />
            <AppBarToggleButton
                x:Name="SnapToggle"
                Icon="Trim"
                IsChecked="True"
                Label="Snap" />
            <AppBarButton
                x:Name="DisconnectButton"
                Icon="Remove"
                Label="Disconnect"
                ToolTipService.ToolTip="Disconnect selected track connections (R)" />
            <AppBarToggleButton
                x:Name="GridToggle"
                Icon="ViewAll"
                IsChecked="False"
                Label="Grid" />
            <AppBarToggleButton
                x:Name="ValidationOverlayToggle"
                IsChecked="True"
                Label="Validation Overlay">
                <AppBarToggleButton.Icon>
                    <SymbolIcon Symbol="Preview" />
                </AppBarToggleButton.Icon>
            </AppBarToggleButton>
            <AppBarToggleButton
                x:Name="TrackLabelsToggle"
                IsChecked="True"
                Label="Track Labels"
                ToolTipService.ToolTip="Show/hide PIKO A track codes (G231, WR, DKW, ...) on the plan">
                <AppBarToggleButton.Icon>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8D2;" />
                </AppBarToggleButton.Icon>
            </AppBarToggleButton>
            <AppBarButton x:Name="RotateLeftButton" Label="Rotate ←">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Back" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton x:Name="RotateRightButton" Label="Rotate →">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Forward" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>
        <!--  Main Content Grid  -->
        <Grid x:Name="MainGrid" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition
                    x:Name="ColToolbox"
                    Width="180"
                    MinWidth="32" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" MinWidth="100" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition
                    x:Name="ColProperties"
                    Width="240"
                    MinWidth="32" />
            </Grid.ColumnDefinitions>
            <!--  Left Panel: Toolbox  -->
            <controls:CollapsibleColumnHelper
                Grid.Column="0"
                Margin="0"
                Glyph="&#xE70F;"
                Header="Piko A-Track"
                IsExpanded="{x:Bind ViewModel.IsToolboxExpanded, Mode=TwoWay}">
                <controls:CollapsibleColumnHelper.PanelContent>
                    <ScrollViewer Padding="8,0">
                        <StackPanel x:Name="ToolboxStackPanel" Spacing="4">
                            <!--  Toolbox is populated from PikoACatalog in OnLoaded  -->
                        </StackPanel>
                    </ScrollViewer>
                </controls:CollapsibleColumnHelper.PanelContent>
            </controls:CollapsibleColumnHelper>

            <controls1:GridSplitter
                Grid.Column="1"
                Width="4"
                HorizontalAlignment="Left"
                ResizeBehavior="PreviousAndNext" />

            <!--  Center: Canvas with Pan/Zoom  -->
            <Border Grid.Column="2" Background="{ThemeResource SolidBackgroundFillColorBaseAltBrush}">
                <Grid>
                    <ScrollViewer
                        x:Name="CanvasScrollViewer"
                        HorizontalScrollBarVisibility="Auto"
                        HorizontalScrollMode="Enabled"
                        MaxZoomFactor="3.0"
                        MinZoomFactor="0.1"
                        VerticalScrollBarVisibility="Auto"
                        VerticalScrollMode="Enabled"
                        ZoomMode="Enabled">
                        <Grid Width="3000" Height="2000">
                            <canvas:CanvasControl
                                x:Name="GraphCanvasControl"
                                Width="3000"
                                Height="2000"
                                Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                                CreateResources="GraphCanvasControl_CreateResources"
                                Draw="GraphCanvasControl_Draw" />
                            <Canvas
                                x:Name="OverlayCanvas"
                                Width="3000"
                                Height="2000"
                                AllowDrop="True"
                                Background="Transparent"
                                IsTabStop="True" />
                        </Grid>
                    </ScrollViewer>
                    <!--  Zoom Control Overlay (Bottom-Right)  -->
                    <StackPanel
                        Margin="0,0,12,12"
                        Padding="12,8"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Orientation="Horizontal"
                        Spacing="8">
                        <Button
                            x:Name="ZoomOutButton"
                            Width="32"
                            Height="32"
                            Content="−"
                            FontSize="16"
                            ToolTipService.ToolTip="Zoom Out (Ctrl+−)" />
                        <Slider
                            x:Name="ZoomSlider"
                            Width="120"
                            VerticalAlignment="Center"
                            Maximum="3.0"
                            Minimum="0.1"
                            Value="1.0" />
                        <Button
                            x:Name="ZoomInButton"
                            Width="32"
                            Height="32"
                            Content="+"
                            FontSize="16"
                            ToolTipService.ToolTip="Zoom In (Ctrl++)" />
                        <TextBlock
                            x:Name="ZoomPercentText"
                            Width="50"
                            VerticalAlignment="Center"
                            Text="100%"
                            TextAlignment="Center" />
                    </StackPanel>
                </Grid>
            </Border>

            <controls1:GridSplitter
                Grid.Column="3"
                Width="4"
                HorizontalAlignment="Left"
                ResizeBehavior="PreviousAndNext" />

            <!--  Right Panel: Properties  -->
            <controls:CollapsibleColumnProperties
                Grid.Column="4"
                Margin="0"
                Glyph="&#xE946;"
                Header="Properties"
                IsExpanded="{x:Bind ViewModel.IsPropertiesExpanded, Mode=TwoWay}">
                <controls:CollapsibleColumnProperties.PanelContent>
                    <ScrollViewer Padding="16">
                        <StackPanel Spacing="16">
                            <!--  Selection Info  -->
                            <StackPanel Spacing="8">
                                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Selection" />
                                <TextBlock
                                    x:Name="SelectionInfoText"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="No selection"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                            <!--  Selected Track Properties (InPort etc.)  -->
                            <StackPanel
                                x:Name="SelectedTrackPropertiesPanel"
                                Spacing="8"
                                Visibility="Collapsed">
                                <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Track Properties" />
                                <NumberBox
                                    x:Name="SelectedTrackInPortBox"
                                    Header="InPort (Z21 feedback address)"
                                    LargeChange="8"
                                    Maximum="4096"
                                    Minimum="0"
                                    PlaceholderText="None"
                                    SmallChange="1"
                                    SpinButtonPlacementMode="Inline"
                                    ToolTipService.ToolTip="Z21 R-BUS feedback address assigned to this track (leave empty for none)"
                                    ValueChanged="OnSelectedTrackInPortChanged" />
                            </StackPanel>
                            <!--  Properties Panel  -->
                            <StackPanel Spacing="12" Visibility="Collapsed">
                                <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Properties" />
                                <TextBox Header="Track ID" IsReadOnly="True" />
                                <TextBox Header="Template" IsReadOnly="True" />
                                <NumberBox Header="Position X (mm)" />
                                <NumberBox Header="Position Y (mm)" />
                                <NumberBox
                                    Header="Rotation (deg)"
                                    Maximum="360"
                                    Minimum="0"
                                    SmallChange="15" />
                                <NumberBox
                                    Header="Feedback Point"
                                    Minimum="0"
                                    PlaceholderText="None" />
                                <Button
                                    Margin="0,8,4,0"
                                    Content="Disconnect"
                                    ToolTipService.ToolTip="Disconnect (R)" />
                                <Button
                                    Margin="0,8,0,0"
                                    Content="Delete"
                                    Style="{ThemeResource AccentButtonStyle}"
                                    ToolTipService.ToolTip="Delete (D/Del)" />
                            </StackPanel>
                            <!--  Sections (Blocks)  -->
                            <StackPanel Spacing="8">
                                <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Sections" />
                                <TextBlock
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="No section assigned"
                                    TextWrapping="Wrap" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button Content="Create Section" IsEnabled="False" />
                                    <Button Content="Toggle Isolator" IsEnabled="False" />
                                </StackPanel>
                                <!--  Section Editor  -->
                                <StackPanel Spacing="8" Visibility="Collapsed">
                                    <TextBox Header="Section Name" PlaceholderText="Enter section name" />
                                    <ComboBox HorizontalAlignment="Stretch" Header="Function">
                                        <ComboBoxItem Content="Track" Tag="Track" />
                                        <ComboBoxItem Content="Station" Tag="Station" />
                                        <ComboBoxItem Content="Siding" Tag="Siding" />
                                        <ComboBoxItem Content="Shunting" Tag="Shunting" />
                                        <ComboBoxItem Content="Main" Tag="Main" />
                                        <ComboBoxItem Content="Branch" Tag="Branch" />
                                    </ComboBox>
                                    <StackPanel>
                                        <TextBlock Margin="0,0,0,4" Text="Color" />
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <Button
                                                Width="28"
                                                Height="28"
                                                Padding="0"
                                                Background="{StaticResource TrackPlanSectionColor1}"
                                                CornerRadius="4"
                                                Tag="#FF5733" />
                                            <Button
                                                Width="28"
                                                Height="28"
                                                Padding="0"
                                                Background="{StaticResource TrackPlanSectionColor2}"
                                                CornerRadius="4"
                                                Tag="#33FF57" />
                                            <Button
                                                Width="28"
                                                Height="28"
                                                Padding="0"
                                                Background="{StaticResource TrackPlanSectionColor3}"
                                                CornerRadius="4"
                                                Tag="#3357FF" />
                                            <Button
                                                Width="28"
                                                Height="28"
                                                Padding="0"
                                                Background="{StaticResource TrackPlanSectionColor4}"
                                                CornerRadius="4"
                                                Tag="#FFFF33" />
                                            <Button
                                                Width="28"
                                                Height="28"
                                                Padding="0"
                                                Background="{StaticResource TrackPlanSectionColor5}"
                                                CornerRadius="4"
                                                Tag="#FF33FF" />
                                            <Button
                                                Width="28"
                                                Height="28"
                                                Padding="0"
                                                Background="{StaticResource TrackPlanSectionColor6}"
                                                CornerRadius="4"
                                                Tag="#33FFFF" />
                                            <Button
                                                Width="28"
                                                Height="28"
                                                Padding="0"
                                                Background="{StaticResource TrackPlanSectionColor7}"
                                                CornerRadius="4"
                                                Tag="#FF9933" />
                                            <Button
                                                Width="28"
                                                Height="28"
                                                Padding="0"
                                                Background="{StaticResource TrackPlanSectionColor8}"
                                                CornerRadius="4"
                                                Tag="#9933FF" />
                                        </StackPanel>
                                    </StackPanel>
                                    <Button Content="Delete Section" Style="{ThemeResource AccentButtonStyle}" />
                                </StackPanel>
                                <ListView MaxHeight="120">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Padding="4" ColumnSpacing="8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="16" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Ellipse
                                                    Grid.Column="0"
                                                    Width="12"
                                                    Height="12"
                                                    Fill="{Binding Color}" />
                                                <TextBlock
                                                    Grid.Column="1"
                                                    VerticalAlignment="Center"
                                                    Text="{Binding Name}" />
                                                <TextBlock
                                                    Grid.Column="2"
                                                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                    Text="{Binding TrackCount}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                            <!--  Statistics  -->
                            <StackPanel Spacing="8">
                                <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Statistics" />
                                <Grid ColumnSpacing="8" RowSpacing="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock
                                        Grid.Row="0"
                                        Grid.Column="0"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="Tracks:" />
                                    <TextBlock
                                        x:Name="NodeCountText"
                                        Grid.Row="0"
                                        Grid.Column="1"
                                        Text="0" />
                                    <TextBlock
                                        Grid.Row="1"
                                        Grid.Column="0"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="Connections:" />
                                    <TextBlock
                                        x:Name="EdgeCountText"
                                        Grid.Row="1"
                                        Grid.Column="1"
                                        Text="0" />
                                    <TextBlock
                                        Grid.Row="2"
                                        Grid.Column="0"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="Open Ends:" />
                                    <TextBlock
                                        x:Name="EndcapCountText"
                                        Grid.Row="2"
                                        Grid.Column="1"
                                        Text="0" />
                                    <TextBlock
                                        Grid.Row="3"
                                        Grid.Column="0"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="Zoom:" />
                                    <TextBlock
                                        x:Name="ZoomLevelText"
                                        Grid.Row="3"
                                        Grid.Column="1"
                                        Text="100%" />
                                </Grid>
                            </StackPanel>
                            <StackPanel x:Name="ValidationLegendPanel" Spacing="8">
                                <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Validation Overlay" />
                                <TextBlock
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Live hints in the canvas for open ends, overlapping ports, and disconnected groups."
                                    TextWrapping="WrapWholeWords" />
                                <StackPanel Spacing="6">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Ellipse
                                            Width="12"
                                            Height="12"
                                            Fill="#FFB900"
                                            Stroke="#FF8C00"
                                            StrokeThickness="1.5" />
                                        <TextBlock VerticalAlignment="Center" Text="Open ends" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Ellipse
                                            Width="16"
                                            Height="16"
                                            Fill="#E81123"
                                            Stroke="#B40014"
                                            StrokeThickness="1.5" />
                                        <TextBlock VerticalAlignment="Center" Text="Disconnected, overlapping ports" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Rectangle
                                            Width="18"
                                            Height="12"
                                            RadiusX="2"
                                            RadiusY="2"
                                            Stroke="#0078D7"
                                            StrokeDashArray="4,2"
                                            StrokeThickness="1.5" />
                                        <TextBlock VerticalAlignment="Center" Text="Disconnected groups" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </controls:CollapsibleColumnProperties.PanelContent>
            </controls:CollapsibleColumnProperties>
        </Grid>
        <!--  Status Bar  -->
        <Border
            Grid.Row="2"
            Padding="16,8"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    x:Name="StatusText"
                    Grid.Column="0"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="" />
                <TextBlock
                    x:Name="CoordinatesText"
                    Grid.Column="1"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    Text="X: 0  Y: 0" />
            </Grid>
        </Border>
    </Grid>
</Page>