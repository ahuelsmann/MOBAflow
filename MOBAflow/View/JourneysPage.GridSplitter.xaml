<Page
    x:Class="Moba.WinUI.View.JourneysPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Moba.WinUI.Controls"
    xmlns:converter="using:Moba.WinUI.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:domain="using:Moba.Domain"
    xmlns:viewmodel="using:Moba.SharedUI.ViewModel"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{x:Bind ViewModel}"
    mc:Ignorable="d">

    <Page.Resources>
        <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid x:Name="MainLayoutGrid">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="LayoutStates">
                <!-- Journey List States -->
                <VisualState x:Name="JourneyListExpanded">
                    <VisualState.Setters>
                        <Setter Target="JourneyListColumn.Width" Value="{x:Bind LayoutViewModel.JourneyListColumn.Width, Mode=OneWay}" />
                        <Setter Target="JourneyListSplitter.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="JourneyListCollapsed">
                    <VisualState.Setters>
                        <Setter Target="JourneyListColumn.Width" Value="0" />
                        <Setter Target="JourneyListSplitter.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
                
                <!-- City Library States -->
                <VisualState x:Name="CityLibraryExpanded">
                    <VisualState.Setters>
                        <Setter Target="CityLibraryColumn.Width" Value="{x:Bind LayoutViewModel.CityLibraryColumn.Width, Mode=OneWay}" />
                        <Setter Target="CityLibrarySplitter.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="CityLibraryCollapsed">
                    <VisualState.Setters>
                        <Setter Target="CityLibraryColumn.Width" Value="0" />
                        <Setter Target="CityLibrarySplitter.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
                
                <!-- Workflow Library States -->
                <VisualState x:Name="WorkflowLibraryExpanded">
                    <VisualState.Setters>
                        <Setter Target="WorkflowLibraryColumn.Width" Value="{x:Bind LayoutViewModel.WorkflowLibraryColumn.Width, Mode=OneWay}" />
                        <Setter Target="WorkflowLibrarySplitter.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WorkflowLibraryCollapsed">
                    <VisualState.Setters>
                        <Setter Target="WorkflowLibraryColumn.Width" Value="0" />
                        <Setter Target="WorkflowLibrarySplitter.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="JourneyListColumn" Width="{x:Bind LayoutViewModel.JourneyListColumn.GridLength, Mode=OneWay}" MinWidth="150" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition x:Name="JourneyContentColumn" Width="{x:Bind LayoutViewModel.JourneyContentColumn.GridLength, Mode=OneWay}" MinWidth="300" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition x:Name="CityLibraryColumn" Width="{x:Bind LayoutViewModel.CityLibraryColumn.GridLength, Mode=OneWay}" MinWidth="200" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition x:Name="WorkflowLibraryColumn" Width="{x:Bind LayoutViewModel.WorkflowLibraryColumn.GridLength, Mode=OneWay}" MinWidth="200" />
        </Grid.ColumnDefinitions>

        <!-- Column 0: Journeys -->
        <Grid Grid.Column="0" Padding="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header: Title + Actions -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    Padding="8,0,0,0"
                    VerticalAlignment="Center"
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    Text="Journeys" />
                <StackPanel
                    Grid.Column="1"
                    Orientation="Horizontal"
                    Spacing="8">
                    <Button
                        Padding="12,8"
                        Command="{x:Bind ViewModel.CreateJourneyCommand}"
                        Content="New Journey"
                        Style="{StaticResource AccentButtonStyle}" />
                </StackPanel>
            </Grid>

            <!-- Search/Filter -->
            <AutoSuggestBox
                x:Name="JourneySearchBox"
                Grid.Row="1"
                Margin="0,0,0,8"
                PlaceholderText="Search journeys..."
                QueryIcon="Find"
                TextChanged="{x:Bind ViewModel.OnJourneySearchTextChanged}" />

            <!-- Journey List -->
            <ListView
                x:Name="JourneyListView"
                Grid.Row="2"
                CanDragItems="True"
                DragItemsStarting="JourneyListView_DragItemsStarting"
                ItemsSource="{x:Bind ViewModel.FilteredJourneys, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.SelectedJourney, Mode=TwoWay}"
                SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="domain:Journey">
                        <Grid Padding="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind Name}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Journey List Splitter -->
        <controls:GridSplitter 
            x:Name="JourneyListSplitter"
            Grid.Column="1" 
            Width="16" 
            HorizontalAlignment="Left"
            ResizeBehavior="BasedOnAlignment"
            ResizeDirection="Columns">
            <controls:GridSplitter.RenderTransform>
                <TranslateTransform X="-7" />
            </controls:GridSplitter.RenderTransform>
        </controls:GridSplitter>

        <!-- Column 2: Journey Content -->
        <Grid Grid.Column="2" Padding="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Journey Details (when selected) -->
            <ScrollViewer
                x:Name="JourneyDetailsScrollViewer"
                Grid.Row="1"
                VerticalScrollBarVisibility="Auto"
                VerticalScrollMode="Auto"
                Visibility="{x:Bind ViewModel.SelectedJourney, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                <StackPanel Spacing="16">
                    <!-- Journey Properties would go here -->
                    <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Journey Details" />
                    <TextBlock Text="{x:Bind ViewModel.SelectedJourney.Name, Mode=OneWay}" />
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- Column 4: City Library (CollapsibleColumn) -->
        <Border Grid.Column="8" Visibility="{x:Bind LayoutViewModel.CityLibraryColumn.Visibility, Mode=OneWay}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" VerticalScrollMode="Auto">
                <StackPanel Spacing="8">
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="Drag or double-click to add" />
                    <ListView
                        x:Name="CityListView"
                        CanDragItems="True"
                        DoubleTapped="CityListView_DoubleTapped"
                        DragItemsStarting="CityListView_DragItemsStarting"
                        ItemsSource="{x:Bind ViewModel.CityLibrary, Mode=OneWay}"
                        SelectedItem="{x:Bind ViewModel.SelectedCity, Mode=TwoWay}"
                        SelectionMode="Single">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="domain:City">
                                <Grid Padding="6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        VerticalAlignment="Center"
                                        Text="{x:Bind Name}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- City Library Splitter -->
        <controls:GridSplitter 
            x:Name="CityLibrarySplitter"
            Grid.Column="9" 
            Width="16" 
            HorizontalAlignment="Left"
            ResizeBehavior="BasedOnAlignment"
            ResizeDirection="Columns"
            Visibility="{x:Bind LayoutViewModel.GetSplitterVisibility('CityLibrarySplitter'), Mode=OneWay}">
            <controls:GridSplitter.RenderTransform>
                <TranslateTransform X="-7" />
            </controls:GridSplitter.RenderTransform>
        </controls:GridSplitter>

        <!-- Column 8: Workflow Library (CollapsibleColumn) -->
        <Border Grid.Column="15" Visibility="{x:Bind LayoutViewModel.WorkflowLibraryColumn.Visibility, Mode=OneWay}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" VerticalScrollMode="Auto">
                <StackPanel Spacing="8">
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="Drag or double-click to add" />
                    <ListView
                        x:Name="WorkflowListView"
                        CanDragItems="True"
                        DoubleTapped="WorkflowListView_DoubleTapped"
                        DragItemsStarting="WorkflowListView_DragItemsStarting"
                        ItemsSource="{x:Bind ViewModel.WorkflowLibrary, Mode=OneWay}"
                        SelectedItem="{x:Bind ViewModel.SelectedWorkflow, Mode=TwoWay}"
                        SelectionMode="Single">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="domain:Workflow">
                                <Grid Padding="6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        VerticalAlignment="Center"
                                        Text="{x:Bind Name}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
