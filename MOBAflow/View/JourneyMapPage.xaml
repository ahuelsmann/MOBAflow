<Page
    x:Class="Moba.WinUI.View.JourneyMapPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Moba.SharedUI.ViewModel"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Page Header  -->
        <StackPanel
            Grid.Row="0"
            Padding="24,16"
            Orientation="Horizontal"
            Spacing="12">
            <TextBlock
                VerticalAlignment="Center"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="Journey Map" />
        </StackPanel>

        <!--  Journey Route Visualization  -->
        <Grid Grid.Row="1" Padding="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Journey Selector  -->
            <StackPanel
                Grid.Row="0"
                Margin="0,0,0,24"
                Orientation="Horizontal"
                Spacing="16">
                <TextBlock
                    VerticalAlignment="Center"
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    Text="Active Journey:" />
                <ComboBox
                    MinWidth="250"
                    ItemsSource="{x:Bind ViewModel.AvailableJourneys, Mode=OneWay}"
                    SelectedItem="{x:Bind ViewModel.SelectedJourney, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="vm:JourneyViewModel">
                            <TextBlock Text="{x:Bind Name, Mode=OneWay}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <!--  Route Visualization  -->
            <Grid
                Grid.Row="1"
                Padding="32"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8">

                <!--  No Journey Selected Placeholder  -->
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="{x:Bind ViewModel.HasSelectedJourney, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                    <FontIcon
                        Margin="0,0,0,16"
                        FontSize="64"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Glyph="&#xE81D;" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Text="No Journey Selected" />
                    <TextBlock
                        Margin="0,8,0,0"
                        HorizontalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Text="Select a journey to visualize the route" />
                </StackPanel>

                <!--  Station Route (Horizontal Layout)  -->
                <ScrollViewer
                    HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Disabled"
                    Visibility="{x:Bind ViewModel.HasSelectedJourney, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!--  Station Names (Top)  -->
                        <ItemsControl
                            Grid.Row="0"
                            Margin="0,0,0,16"
                            ItemsSource="{x:Bind ViewModel.RouteStations, Mode=OneWay}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="0" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:StationViewModel">
                                    <TextBlock
                                        Width="120"
                                        FontSize="12"
                                        FontWeight="{x:Bind IsCurrentStation, Mode=OneWay, Converter={StaticResource BoolToFontWeightConverter}}"
                                        Foreground="{x:Bind IsCurrentStation, Mode=OneWay, Converter={StaticResource BoolToAccentBrushConverter}}"
                                        Text="{x:Bind Name, Mode=OneWay}"
                                        TextAlignment="Center"
                                        TextTrimming="CharacterEllipsis"
                                        TextWrapping="NoWrap" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!--  Track Line with Station Dots  -->
                        <ItemsControl Grid.Row="1" ItemsSource="{x:Bind ViewModel.RouteStations, Mode=OneWay}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="0" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:StationViewModel">
                                    <Grid Width="120" Height="40">
                                        <!--  Track segment (line to next station)  -->
                                        <Rectangle
                                            Height="4"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Center"
                                            Fill="{ThemeResource ControlStrongStrokeColorDefaultBrush}" />

                                        <!--  Station dot  -->
                                        <Ellipse
                                            Width="20"
                                            Height="20"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Fill="{x:Bind IsCurrentStation, Mode=OneWay, Converter={StaticResource BoolToAccentBrushConverter}}"
                                            Stroke="{ThemeResource ControlStrongStrokeColorDefaultBrush}"
                                            StrokeThickness="2" />

                                        <!--  Train icon on current station  -->
                                        <FontIcon
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontSize="12"
                                            Foreground="White"
                                            Glyph="&#xE81D;"
                                            Visibility="{x:Bind IsCurrentStation, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!--  Additional Info (Bottom)  -->
                        <ItemsControl
                            Grid.Row="2"
                            Margin="0,8,0,0"
                            ItemsSource="{x:Bind ViewModel.RouteStations, Mode=OneWay}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="0" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:StationViewModel">
                                    <StackPanel Width="120" Spacing="2">
                                        <TextBlock
                                            FontSize="10"
                                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                            HorizontalTextAlignment="Center"
                                            Text="{x:Bind Track, Mode=OneWay, Converter={StaticResource TrackNumberConverter}}" />
                                        <TextBlock
                                            FontSize="10"
                                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                            HorizontalTextAlignment="Center"
                                            Text="{x:Bind IsExitOnLeft, Mode=OneWay, Converter={StaticResource ExitSideConverter}}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Grid>

        <!--  Status Bar  -->
        <Grid
            Grid.Row="2"
            Padding="24,12"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Journey Progress  -->
            <StackPanel
                Grid.Column="0"
                Orientation="Horizontal"
                Spacing="8">
                <FontIcon
                    FontSize="16"
                    Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                    Glyph="&#xE8B5;" />
                <TextBlock
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Progress:" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontWeight="SemiBold"
                    Text="{x:Bind ViewModel.ProgressText, Mode=OneWay}" />
            </StackPanel>

            <!--  Current Counter  -->
            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                Spacing="8">
                <FontIcon
                    FontSize="16"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Glyph="&#xE8EE;" />
                <TextBlock
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Counter:" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontWeight="SemiBold"
                    Text="{x:Bind ViewModel.CounterText, Mode=OneWay}" />
            </StackPanel>

            <!--  Behavior on Last Stop  -->
            <StackPanel
                Grid.Column="2"
                Orientation="Horizontal"
                Spacing="8">
                <FontIcon
                    FontSize="16"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Glyph="&#xE8AB;" />
                <TextBlock
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="On Last Stop:" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontWeight="SemiBold"
                    Text="{x:Bind ViewModel.BehaviorOnLastStopText, Mode=OneWay}" />
            </StackPanel>

            <!--  InPort  -->
            <StackPanel
                Grid.Column="4"
                Orientation="Horizontal"
                Spacing="8">
                <TextBlock
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    Text="InPort:" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontWeight="SemiBold"
                    Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                    Text="{x:Bind ViewModel.JourneyInPort, Mode=OneWay}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>