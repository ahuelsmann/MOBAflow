<Page
    x:Class="Moba.WinUI.View.TrainControlPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Moba.WinUI.Controls"
    xmlns:converter="using:Moba.WinUI.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Moba.SharedUI.ViewModel"
    DataContext="{x:Bind ViewModel}"
    mc:Ignorable="d">

    <Page.Resources>
        <converter:BoolToBacklightBrushConverter x:Key="BacklightConverter" />
        <converter:BoolToPickerStateBrushConverter x:Key="PickerBackgroundConverter" Role="Background" />
        <converter:BoolToPickerStateBrushConverter x:Key="PickerBorderConverter" Role="Border" />
        <converter:BoolToPickerStateBrushConverter x:Key="PickerForegroundConverter" Role="Foreground" />
        <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converter:MillivoltsToVoltsConverter x:Key="MillivoltsToVoltsConverter" />
    </Page.Resources>

    <Grid x:Name="RootGrid" Padding="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Page Header - Fluent Design System with elevated background  -->
        <Border
            Grid.Row="0"
            Background="{ThemeResource AcrylicInAppFillColorDefaultBrush}"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="0,0,0,1">
            <Grid Padding="32,20,32,20" ColumnSpacing="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="150" />
                    <!--  Title  -->
                    <ColumnDefinition Width="*" MinWidth="250" />
                    <!--  Baureihe + Vmax  -->
                    <ColumnDefinition Width="Auto" MinWidth="80" />
                    <!--  Emergency Stop  -->
                </Grid.ColumnDefinitions>

                <StackPanel
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Spacing="4">
                    <TextBlock
                        AutomationProperties.HeadingLevel="Level1"
                        FontSize="32"
                        FontWeight="SemiBold"
                        Text="Train Control" />
                </StackPanel>

                <!--  Locomotive Series Selection (Baureihe)  -->
                <StackPanel
                    Grid.Column="1"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="12">
                    <AutoSuggestBox
                        x:Name="LocoSeriesBox"
                        Width="220"
                        VerticalAlignment="Center"
                        AutomationProperties.HelpText="Enter locomotive series to set maximum speed"
                        AutomationProperties.Name="Locomotive Series Selection"
                        PlaceholderText="Series (e.g. BR 103)"
                        QuerySubmitted="LocoSeriesBox_QuerySubmitted"
                        SuggestionChosen="LocoSeriesBox_SuggestionChosen"
                        TextChanged="LocoSeriesBox_TextChanged">
                        <AutoSuggestBox.QueryIcon>
                            <FontIcon FontSize="12" Glyph="&#xE7C0;" />
                        </AutoSuggestBox.QueryIcon>
                    </AutoSuggestBox>
                    <StackPanel
                        x:Name="VmaxDisplay"
                        VerticalAlignment="Center"
                        AutomationProperties.LiveSetting="Polite"
                        Orientation="Horizontal"
                        Spacing="4"
                        Visibility="Collapsed">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="12"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="Vmax:" />
                        <TextBlock
                            x:Name="VmaxText"
                            VerticalAlignment="Center"
                            AutomationProperties.Name="Maximum Speed in kilometers per hour"
                            FontSize="12"
                            FontWeight="SemiBold"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            Text="0" />
                    </StackPanel>
                </StackPanel>

                <Button
                    Grid.Column="2"
                    Padding="16,8"
                    AccessKey="S"
                    AutomationProperties.HelpText="Immediately stops the locomotive"
                    AutomationProperties.Name="Emergency Stop"
                    Command="{x:Bind ViewModel.EmergencyStopCommand}"
                    Style="{StaticResource DangerButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon
                            FontSize="18"
                            Foreground="{ThemeResource MobaDangerButtonForegroundBrush}"
                            Glyph="&#xE711;" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="14"
                            FontWeight="Bold"
                            Foreground="{ThemeResource MobaDangerButtonForegroundBrush}"
                            Text="EMERGENCY STOP" />
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!--  Main Content with Responsive Layout - Fluent Design  -->
        <Grid
            Grid.Row="1"
            Padding="32,24,32,32"
            ColumnSpacing="0"
            RowSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="300" />
            </Grid.ColumnDefinitions>

            <!--  LEFT PANEL: Locomotive Settings (Grid.Column="0")  -->
            <Border
                Grid.Column="0"
                Padding="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <ScrollViewer Padding="0,0,8,0" VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="20">
                        <!--  Header  -->
                        <StackPanel Spacing="2">
                            <TextBlock
                                FontSize="16"
                                FontWeight="SemiBold"
                                Text="Locomotive" />
                            <TextBlock
                                FontSize="11"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Select locomotive and control" />
                        </StackPanel>

                        <StackPanel Spacing="6">
                            <TextBlock
                                FontSize="11"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Text="LOCOMOTIVE" />
                            <ItemsRepeater ItemsSource="{x:Bind ViewModel.ProjectLocomotives, Mode=OneWay}">
                                <ItemsRepeater.Layout>
                                    <StackLayout Orientation="Vertical" Spacing="8" />
                                </ItemsRepeater.Layout>
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="vm:LocomotiveViewModel">
                                        <Button
                                            Padding="0"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Stretch"
                                            AutomationProperties.Name="{x:Bind Name, Mode=OneWay}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Command="{Binding DataContext.SelectProjectLocomotiveCommand, ElementName=PageRoot}"
                                            CommandParameter="{x:Bind}">
                                            <Border
                                                x:Name="PickerItemRoot"
                                                Padding="8"
                                                Background="{x:Bind IsPickerSelected, Mode=OneWay, Converter={StaticResource PickerBackgroundConverter}, ConverterParameter={Binding ElementName=PageRoot}}"
                                                BorderBrush="{x:Bind IsPickerSelected, Mode=OneWay, Converter={StaticResource PickerBorderConverter}, ConverterParameter={Binding ElementName=PageRoot}}"
                                                BorderThickness="1"
                                                CornerRadius="8">
                                                <Grid ColumnSpacing="12">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <Border
                                                        Width="56"
                                                        Height="56"
                                                        Background="{ThemeResource LayerFillColorDefaultBrush}"
                                                        CornerRadius="4">
                                                        <Grid>
                                                            <Image
                                                                Source="{x:Bind PhotoPathWithVersion, Mode=OneWay, Converter={StaticResource PhotoPathToImageConverter}}"
                                                                Stretch="UniformToFill"
                                                                Visibility="{x:Bind HasPhoto, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                            <FontIcon
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"
                                                                FontSize="20"
                                                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                                Glyph="&#xE7C0;"
                                                                Visibility="{x:Bind HasPhoto, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}" />
                                                        </Grid>
                                                    </Border>
                                                    <TextBlock
                                                        x:Name="LocoNameText"
                                                        Grid.Column="1"
                                                        VerticalAlignment="Center"
                                                        FontWeight="SemiBold"
                                                        Foreground="{x:Bind IsPickerSelected, Mode=OneWay, Converter={StaticResource PickerForegroundConverter}, ConverterParameter={Binding ElementName=PageRoot}}"
                                                        Text="{x:Bind Name, Mode=OneWay}"
                                                        TextTrimming="CharacterEllipsis"
                                                        TextWrapping="NoWrap" />
                                                </Grid>
                                            </Border>
                                        </Button>
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>
                            <TextBlock
                                FontSize="11"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="No locomotives in project. Add locomotives on the Locomotives page."
                                TextWrapping="WrapWholeWords"
                                Visibility="{x:Bind ViewModel.HasProjectLocomotives, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}" />
                        </StackPanel>

                        <!--  Direction  -->
                        <StackPanel Spacing="6">
                            <TextBlock
                                FontSize="11"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Text="DIRECTION" />
                            <ToggleSwitch
                                IsOn="{x:Bind ViewModel.IsForward, Mode=TwoWay}"
                                OffContent="Backward"
                                OnContent="Forward" />
                        </StackPanel>

                        <!--  DCC Speed Steps  -->
                        <StackPanel Spacing="6">
                            <TextBlock
                                FontSize="11"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Text="DCC SPEED STEPS" />
                            <ComboBox
                                HorizontalAlignment="Stretch"
                                SelectedIndex="{x:Bind SpeedStepsSelectedIndex, Mode=TwoWay}"
                                SelectionChanged="SpeedStepsComboBox_SelectionChanged">
                                <ComboBoxItem Content="14 Steps" Tag="14" />
                                <ComboBoxItem Content="28 Steps" Tag="28" />
                                <ComboBoxItem Content="128 Steps" Tag="128" />
                            </ComboBox>
                        </StackPanel>

                        <!--  Separator  -->
                        <Border
                            Height="1"
                            Margin="0,4"
                            Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

                        <!--  Acceleration Ramp  -->
                        <StackPanel Spacing="6">
                            <TextBlock
                                FontSize="11"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Text="ACCELERATION RAMP" />
                            <ToggleSwitch
                                IsOn="{x:Bind ViewModel.IsRampEnabled, Mode=TwoWay}"
                                OffContent="Instant"
                                OnContent="Gradual" />
                            <Grid ColumnSpacing="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Row="0" Spacing="4">
                                    <TextBlock
                                        FontSize="10"
                                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                        Text="Step Size" />
                                    <NumberBox
                                        IsEnabled="{x:Bind ViewModel.IsRampEnabled, Mode=OneWay}"
                                        Maximum="20"
                                        Minimum="1"
                                        SmallChange="1"
                                        SpinButtonPlacementMode="Compact"
                                        Value="{x:Bind ViewModel.RampStepSize, Mode=TwoWay}" />
                                </StackPanel>
                                <StackPanel Grid.Row="1" Spacing="4">
                                    <TextBlock
                                        FontSize="10"
                                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                        Text="Interval (ms)" />
                                    <NumberBox
                                        IsEnabled="{x:Bind ViewModel.IsRampEnabled, Mode=OneWay}"
                                        Maximum="500"
                                        Minimum="50"
                                        SmallChange="10"
                                        SpinButtonPlacementMode="Compact"
                                        Value="{x:Bind ViewModel.RampIntervalMs, Mode=TwoWay}" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>

                        <!--  Ramping Indicator  -->
                        <Border
                            Padding="8"
                            Background="{ThemeResource SystemFillColorCautionBackgroundBrush}"
                            CornerRadius="4"
                            Visibility="{x:Bind ViewModel.IsRamping, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <ProgressRing
                                    Width="14"
                                    Height="14"
                                    IsActive="True" />
                                <TextBlock FontSize="11" Text="Ramping..." />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!--  CENTER: 2x2 Grid Layout (Grid.Column="1")  -->
            <Grid
                Grid.Column="1"
                ColumnSpacing="16"
                RowSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  Bereich 1 (Bottom-Left): Speedometer  -->
                <Border
                    Grid.Row="1"
                    Grid.Column="0"
                    Padding="16"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!--  Speedometer  -->
                        <controls:SpeedometerControl
                            x:Name="Speedometer"
                            Grid.Row="0"
                            GaugeMaxKmh="400"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            DisplayValue="{x:Bind ViewModel.SpeedKmh, Mode=OneWay}"
                            MinValue="0"
                            Value="{x:Bind ViewModel.Speed, Mode=OneWay}" />

                        <!--  Slider and Presets (below Speedometer)  -->
                        <StackPanel Grid.Row="1" Spacing="16">
                            <!--  Horizontal Speed Slider  -->
                            <StackPanel Spacing="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        VerticalAlignment="Center"
                                        FontSize="12"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="0" />
                                    <Slider
                                        Grid.Column="1"
                                        Margin="16,0"
                                        IsEnabled="{x:Bind ViewModel.IsSpeedControlEnabled, Mode=OneWay}"
                                        Maximum="{x:Bind ViewModel.MaxSpeedStep, Mode=OneWay}"
                                        Minimum="0"
                                        TickFrequency="10"
                                        TickPlacement="BottomRight"
                                        Value="{x:Bind ViewModel.Speed, Mode=TwoWay}" />
                                    <TextBlock
                                        Grid.Column="2"
                                        VerticalAlignment="Center"
                                        FontSize="12"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{x:Bind ViewModel.MaxSpeedStep, Mode=OneWay}" />
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <!--  Bereich 2 (Top-Left): Journey Info / Timetable Stops  -->
                <controls:TimetableStopsControl
                    Grid.Row="0"
                    Grid.Column="0"
                    CurrentStationArrivalValue="{x:Bind ViewModel.CurrentStationArrival, Mode=OneWay}"
                    CurrentStationDepartureValue="{x:Bind ViewModel.CurrentStationDeparture, Mode=OneWay}"
                    CurrentStationHasValue="{x:Bind ViewModel.CurrentStationHasValue, Mode=OneWay}"
                    CurrentStationIsExitOnLeft="{x:Bind ViewModel.CurrentStationIsExitOnLeft, Mode=OneWay}"
                    CurrentStationNameValue="{x:Bind ViewModel.CurrentStationName, Mode=OneWay}"
                    CurrentStationTrackValue="{x:Bind ViewModel.CurrentStationTrack, Mode=OneWay}"
                    NextStationArrivalValue="{x:Bind ViewModel.NextStationArrival, Mode=OneWay}"
                    NextStationDepartureValue="{x:Bind ViewModel.NextStationDeparture, Mode=OneWay}"
                    NextStationHasValue="{x:Bind ViewModel.NextStationHasValue, Mode=OneWay}"
                    NextStationIsExitOnLeft="{x:Bind ViewModel.NextStationIsExitOnLeft, Mode=OneWay}"
                    NextStationNameValue="{x:Bind ViewModel.NextStationName, Mode=OneWay}"
                    NextStationTrackValue="{x:Bind ViewModel.NextStationTrack, Mode=OneWay}"
                    PreviousStationArrivalValue="{x:Bind ViewModel.PreviousStationArrival, Mode=OneWay}"
                    PreviousStationDepartureValue="{x:Bind ViewModel.PreviousStationDeparture, Mode=OneWay}"
                    PreviousStationHasValue="{x:Bind ViewModel.PreviousStationHasValue, Mode=OneWay}"
                    PreviousStationIsExitOnLeft="{x:Bind ViewModel.PreviousStationIsExitOnLeft, Mode=OneWay}"
                    PreviousStationNameValue="{x:Bind ViewModel.PreviousStationName, Mode=OneWay}"
                    PreviousStationTrackValue="{x:Bind ViewModel.PreviousStationTrack, Mode=OneWay}" />

                <!--  Bereich 3 (Bottom-Right): Amperemeter (Main Track Current)  -->
                <Border
                    Grid.Row="1"
                    Grid.Column="1"
                    Padding="16"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!--  Header with Peak Badge  -->
                        <Grid
                            Grid.Row="0"
                            Margin="0,0,0,8"
                            ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="12">
                                <Border
                                    Padding="8,4"
                                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10">
                                    <StackPanel
                                        VerticalAlignment="Center"
                                        Orientation="Horizontal"
                                        Spacing="6">
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontSize="10"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Text="MAX" />
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontSize="10"
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                            Text="{x:Bind ViewModel.PeakMainCurrent, Mode=OneWay}" />
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontSize="10"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Text="mA" />
                                    </StackPanel>
                                </Border>
                                <Border
                                    Padding="8,4"
                                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10">
                                    <StackPanel
                                        VerticalAlignment="Center"
                                        Orientation="Horizontal"
                                        Spacing="6">
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontSize="10"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Text="MAX" />
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontSize="10"
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                            Text="{x:Bind ViewModel.PeakTemperature, Mode=OneWay}" />
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontSize="10"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Text="°C" />
                                    </StackPanel>
                                </Border>
                                <Button
                                    Padding="4"
                                    VerticalAlignment="Center"
                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    Command="{x:Bind ViewModel.ResetPeakCurrentCommand}"
                                    CornerRadius="6"
                                    ToolTipService.ToolTip="Reset peak current and peak temperature">
                                    <FontIcon
                                        FontSize="12"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Glyph="&#xE711;" />
                                </Button>
                            </StackPanel>
                        </Grid>

                        <!--  Amperemeter  -->
                        <controls:AmperemeterControl
                            x:Name="Amperemeter"
                            Grid.Row="1"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            MaxValue="3000"
                            MinValue="0"
                            Value="{x:Bind ViewModel.MainTrackCurrent, Mode=OneWay}" />

                        <!--  Additional Info (below Amperemeter)  -->
                        <StackPanel Grid.Row="2" Spacing="8">
                            <Border
                                Padding="8"
                                Background="{ThemeResource LayerFillColorDefaultBrush}"
                                CornerRadius="4">
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <!--  Supply Voltage  -->
                                    <StackPanel Grid.Column="0" Spacing="2">
                                        <TextBlock
                                            FontSize="9"
                                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                            Text="SUPPLY" />
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <TextBlock
                                                FontSize="12"
                                                FontWeight="SemiBold"
                                                Text="{x:Bind ViewModel.SupplyVoltage, Mode=OneWay, Converter={StaticResource MillivoltsToVoltsConverter}}" />
                                            <TextBlock
                                                FontSize="10"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                Text="V" />
                                        </StackPanel>
                                    </StackPanel>

                                    <!--  Temperature  -->
                                    <StackPanel
                                        Grid.Column="1"
                                        HorizontalAlignment="Right"
                                        Spacing="2">
                                        <TextBlock
                                            FontSize="9"
                                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                            Text="TEMP"
                                            TextAlignment="Right" />
                                        <StackPanel
                                            HorizontalAlignment="Right"
                                            Orientation="Horizontal"
                                            Spacing="4">
                                            <TextBlock
                                                FontSize="12"
                                                FontWeight="SemiBold"
                                                Text="{x:Bind ViewModel.Temperature, Mode=OneWay}"
                                                TextAlignment="Right" />
                                            <TextBlock
                                                FontSize="10"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                Text="°C"
                                                TextAlignment="Right" />
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <!--  Bereich 4 (Top-Right): Functions  -->
                <Border Grid.Row="0" Grid.Column="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <!--  Brake: red = active (icon yellow with exclamation mark), green = released (icon white/black per theme, no exclamation mark)  -->
                        <Button
                            Grid.Row="0"
                            Grid.Column="0"
                            Background="{x:Bind ViewModel.BrakeButtonColorHex, Mode=OneWay, Converter={StaticResource HexColorToBrushConverter}}"
                            Command="{x:Bind ViewModel.ToggleBrakeCommand}"
                            ContentTemplate="{x:Bind BrakeIconTemplate, Mode=OneWay}"
                            IsEnabled="{x:Bind ViewModel.IsBrakeButtonEnabled, Mode=OneWay}"
                            Style="{StaticResource NeonGlowButtonStyle}" />
                        <!--  Door release: green/DoorOpen = released, red/DoorClose = locked; only clickable when brake active (red); 5s yellow with blink  -->
                        <Button
                            Grid.Row="0"
                            Grid.Column="1"
                            Background="{x:Bind ViewModel.DoorReleaseButtonColorHex, Mode=OneWay, Converter={StaticResource HexColorToBrushConverter}}"
                            Command="{x:Bind ViewModel.ToggleDoorReleaseCommand}"
                            ContentTemplate="{x:Bind DoorReleaseIconTemplate, Mode=OneWay}"
                            IsEnabled="{x:Bind ViewModel.IsDoorReleaseButtonEnabled, Mode=OneWay}"
                            Opacity="{x:Bind ViewModel.DoorReleaseBlinkOpacity, Mode=OneWay}"
                            Style="{StaticResource NeonGlowButtonStyle}" />
                        <Button
                            Grid.Row="0"
                            Grid.Column="2"
                            AutomationProperties.Name="Door blocking indicator"
                            Background="{x:Bind ViewModel.DoorBlockedIndicatorColorHex, Mode=OneWay, Converter={StaticResource HexColorToBrushConverter}}"
                            Content="{x:Bind ViewModel.DoorBlockedIndicatorIconColorHex, Mode=OneWay}"
                            ContentTemplate="{x:Bind DoorBlockedIconTemplate, Mode=OneWay}"
                            IsHitTestVisible="False"
                            IsTabStop="False"
                            Opacity="{x:Bind ViewModel.DoorBlockedIndicatorOpacity, Mode=OneWay}"
                            Style="{StaticResource NeonGlowButtonStyle}" />
                    </Grid>
                </Border>
            </Grid>

            <!--  RIGHT PANEL: Functions F0-F31 (Grid.Column="2") - Hidden on Medium/Compact  -->
            <Border
                Grid.Column="2"
                Padding="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="12">
                        <!--  Header  -->
                        <TextBlock
                            FontSize="11"
                            FontWeight="SemiBold"
                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                            Text="FUNCTIONS" />

                        <ItemsRepeater ItemsSource="{x:Bind ViewModel.Functions, Mode=OneWay}">
                            <ItemsRepeater.Layout>
                                <StackLayout Orientation="Vertical" Spacing="3" />
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="vm:FunctionButtonViewModel">
                                    <controls:FunctionToggleControl
                                        BacklightColorHex="{x:Bind BacklightColorHex, Mode=OneWay}"
                                        Description="{x:Bind Description, Mode=OneWay}"
                                        EditAppearanceCommand="{Binding DataContext.EditFunctionAppearanceCommand, ElementName=PageRoot}"
                                        FunctionIndex="{x:Bind Index, Mode=OneWay}"
                                        IconAsset="{x:Bind IconAsset, Mode=OneWay}"
                                        IsOn="{x:Bind IsOn, Mode=OneWay}"
                                        Label="{x:Bind Label, Mode=OneWay}"
                                        ToggleCommand="{Binding DataContext.ToggleFunctionCommand, ElementName=PageRoot}" />
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>


