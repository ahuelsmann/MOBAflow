<Page
    x:Class="Moba.WinUI.View.JourneysPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Moba.WinUI.Controls"
    xmlns:converter="using:Moba.WinUI.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:domain="using:Moba.Domain"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Moba.WinUI.Behavior"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="using:Moba.SharedUI.ViewModel"
    DataContext="{x:Bind ViewModel}"
    mc:Ignorable="d">

    <Page.Resources>
        <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" MinWidth="200" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="250" MinWidth="200" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <!--  Properties Panel  -->
            <ColumnDefinition
                Width="*"
                MinWidth="350"
                MaxWidth="800" />
        </Grid.ColumnDefinitions>

        <!--  Column 0: Journeys  -->
        <Grid Grid.Column="0" Padding="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Header: Title + Actions  -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    Padding="8,0,0,0"
                    VerticalAlignment="Center"
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    Text="Journeys" />
                <StackPanel
                    Grid.Column="1"
                    Orientation="Horizontal"
                    Spacing="2">
                    <Button
                        Padding="4"
                        Command="{x:Bind ViewModel.AddJourneyCommand}"
                        ToolTipService.ToolTip="Create a new journey">
                        <FontIcon FontSize="14" Glyph="&#xE710;" />
                    </Button>
                    <Button
                        Padding="4"
                        Command="{x:Bind ViewModel.DeleteJourneyCommand}"
                        ToolTipService.ToolTip="Remove the selected journey">
                        <FontIcon FontSize="14" Glyph="&#xE74D;" />
                    </Button>
                </StackPanel>
            </Grid>

            <AutoSuggestBox
                Grid.Row="1"
                Margin="0,0,0,8"
                PlaceholderText="Search journeys..."
                QueryIcon="Find"
                Text="{x:Bind ViewModel.JourneySearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <ListView
                Grid.Row="2"
                IsItemClickEnabled="True"
                ItemsSource="{x:Bind ViewModel.SelectedProject.Journeys, Mode=OneWay}"
                KeyDown="JourneysListView_KeyDown"
                SelectedItem="{x:Bind ViewModel.SelectedJourney, Mode=TwoWay}"
                SelectionMode="Single">
                <i:Interaction.Behaviors>
                    <local:ListViewItemClickBehavior Command="{x:Bind ViewModel.JourneysPageItemClickedCommand}" />
                </i:Interaction.Behaviors>
                <ListView.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Command="{x:Bind ViewModel.AddJourneyCommand}" Text="Add Journey">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE710;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Command="{x:Bind ViewModel.DeleteJourneyCommand}" Text="Delete Journey">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE738;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </ListView.ContextFlyout>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="viewmodel:JourneyViewModel">
                        <Grid Padding="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <FontIcon
                                Grid.Column="0"
                                Margin="0,0,8,0"
                                FontSize="16"
                                Glyph="&#xE7C1;" />
                            <TextBlock
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Text="{x:Bind Name, Mode=OneWay}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!--  Column 1: Splitter  -->
        <Grid
            Grid.Column="1"
            Width="5"
            Background="Transparent"
            PointerEntered="OnSplitterPointerEntered"
            PointerExited="OnSplitterPointerExited"
            PointerMoved="OnSplitterPointerMoved"
            PointerPressed="OnSplitterPointerPressed"
            PointerReleased="OnSplitterPointerReleased"
            Tag="Splitter1">
            <Grid
                Width="1"
                HorizontalAlignment="Center"
                Background="{ThemeResource DividerStrokeColorDefaultBrush}" />
        </Grid>

        <!--  Column 2: Stations (of selected Journey)  -->
        <Grid Grid.Column="2" Padding="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Header: Title + Actions  -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    Padding="8,0,0,0"
                    VerticalAlignment="Center"
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    Text="Stations" />
                <StackPanel
                    Grid.Column="1"
                    Orientation="Horizontal"
                    Spacing="2">
                    <Button
                        Padding="4"
                        Command="{x:Bind ViewModel.AddStationCommand}"
                        ToolTipService.ToolTip="Add a new station to this journey">
                        <FontIcon FontSize="14" Glyph="&#xE710;" />
                    </Button>
                    <Button
                        Padding="4"
                        Command="{x:Bind ViewModel.DeleteStationCommand}"
                        ToolTipService.ToolTip="Remove the selected station">
                        <FontIcon FontSize="14" Glyph="&#xE74D;" />
                    </Button>
                </StackPanel>
            </Grid>

            <ListView
                Grid.Row="1"
                AllowDrop="True"
                DragOver="StationListView_DragOver"
                Drop="StationListView_Drop"
                IsItemClickEnabled="True"
                ItemsSource="{x:Bind ViewModel.SelectedJourney.Stations, Mode=OneWay}"
                KeyDown="StationsListView_KeyDown"
                SelectedItem="{x:Bind ViewModel.SelectedStation, Mode=TwoWay}"
                SelectionMode="Single">
                <i:Interaction.Behaviors>
                    <local:ListViewItemClickBehavior Command="{x:Bind ViewModel.JourneysPageItemClickedCommand}" />
                </i:Interaction.Behaviors>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="viewmodel:StationViewModel">
                        <Grid Padding="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!--  Current Station Indicator  -->
                            <Border
                                Grid.ColumnSpan="2"
                                Margin="-6"
                                Background="{ThemeResource SystemAccentColorLight2}"
                                CornerRadius="4"
                                Opacity="0.3"
                                Visibility="{x:Bind IsCurrentStation, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                            <FontIcon
                                Grid.Column="0"
                                Margin="0,0,8,0"
                                FontSize="16"
                                Glyph="&#xE81D;" />
                            <TextBlock
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Text="{x:Bind Name, Mode=OneWay}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!--  Column 3: Splitter  -->
        <Grid
            Grid.Column="3"
            Width="5"
            Background="Transparent"
            PointerEntered="OnSplitterPointerEntered"
            PointerExited="OnSplitterPointerExited"
            PointerMoved="OnSplitterPointerMoved"
            PointerPressed="OnSplitterPointerPressed"
            PointerReleased="OnSplitterPointerReleased"
            Tag="Splitter2">
            <Grid
                Width="1"
                HorizontalAlignment="Center"
                Background="{ThemeResource DividerStrokeColorDefaultBrush}" />
        </Grid>

        <!--  Column 4: City Library (CollapsibleColumn)  -->
        <controls:CollapsibleColumn
            Grid.Column="4"
            Margin="4,8"
            Glyph="&#xEC06;"
            Header="City Library"
            IsExpanded="{x:Bind ViewModel.IsCityLibraryVisible, Mode=TwoWay}">
            <controls:CollapsibleColumn.PanelContent>
                <ScrollViewer VerticalScrollBarVisibility="Auto" VerticalScrollMode="Auto">
                    <StackPanel Spacing="8">
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="Drag or double-click to add" />
                        <ListView
                            CanDragItems="True"
                            DoubleTapped="CityListView_DoubleTapped"
                            DragItemsStarting="CityListView_DragItemsStarting"
                            ItemsSource="{x:Bind ViewModel.CityLibrary, Mode=OneWay}"
                            SelectedItem="{x:Bind ViewModel.SelectedCity, Mode=TwoWay}"
                            SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="domain:City">
                                    <Grid Padding="6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock
                                            Grid.Column="0"
                                            VerticalAlignment="Center"
                                            Text="{x:Bind Name}" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </ScrollViewer>
            </controls:CollapsibleColumn.PanelContent>
        </controls:CollapsibleColumn>

        <!--  Column 6: Workflow Library (CollapsibleColumn)  -->
        <controls:CollapsibleColumn
            Grid.Column="6"
            Margin="4,8"
            Glyph="&#xE945;"
            Header="Workflow Library"
            IsExpanded="{x:Bind ViewModel.IsWorkflowLibraryVisible, Mode=TwoWay}">
            <controls:CollapsibleColumn.PanelContent>
                <ScrollViewer VerticalScrollBarVisibility="Auto" VerticalScrollMode="Auto">
                    <StackPanel Spacing="8">
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="Drag to station to assign" />
                        <ListView
                            CanDragItems="True"
                            DragItemsStarting="WorkflowListView_DragItemsStarting"
                            ItemsSource="{x:Bind ViewModel.SelectedProject.Workflows, Mode=OneWay}"
                            SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="viewmodel:WorkflowViewModel">
                                    <Grid Padding="6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock
                                            Grid.Column="0"
                                            VerticalAlignment="Center"
                                            Text="{x:Bind Name, Mode=OneWay}" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </ScrollViewer>
            </controls:CollapsibleColumn.PanelContent>
        </controls:CollapsibleColumn>

        <!--  Column 7: Properties Panel  -->
        <Grid Grid.Column="7" Padding="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <ContentControl
                Grid.Row="0"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                Content="{x:Bind ViewModel.JourneysPageSelectedObject, Mode=OneWay}"
                ContentTemplateSelector="{StaticResource EntityTemplateSelector}" />
        </Grid>
    </Grid>
</Page>