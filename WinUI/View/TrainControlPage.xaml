<Page
    x:Class="Moba.WinUI.View.TrainControlPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Moba.WinUI.Controls"
    xmlns:converter="using:Moba.WinUI.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{x:Bind ViewModel}"
    mc:Ignorable="d">

    <Page.Resources>
        <converter:BoolToGlowOpacityConverter x:Key="GlowOpacityConverter" />
    </Page.Resources>

    <Grid Padding="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Page Header  -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    FontSize="28"
                    FontWeight="SemiBold"
                    Text="Train Control" />
                <Button
                    Grid.Column="2"
                    Height="48"
                    Padding="24,0"
                    Background="#E81123"
                    Command="{x:Bind ViewModel.EmergencyStopCommand}"
                    CornerRadius="8">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon
                            FontSize="20"
                            Foreground="White"
                            Glyph="&#xE711;" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="16"
                            FontWeight="Bold"
                            Foreground="White"
                            Text="EMERGENCY STOP" />
                    </StackPanel>
                </Button>
            </Grid>

                <!--  Main Content: 4 Columns - Left Panel | Speedometer | Functions | Speed Categories  -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="140" />
                        <ColumnDefinition Width="200" />
                    </Grid.ColumnDefinitions>

                    <!--  LEFT PANEL: Locomotive Settings  -->
                    <Border
                        Grid.Column="0"
                        Margin="0,0,8,0"
                        Padding="12,12,4,12"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <ScrollViewer Padding="0,0,12,0" VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="20">
                        <!--  Header  -->
                        <StackPanel Spacing="2">
                            <TextBlock
                                FontSize="16"
                                FontWeight="SemiBold"
                                Text="Locomotive" />
                            <TextBlock
                                FontSize="11"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Address and functions" />
                        </StackPanel>

                        <!--  DCC Address  -->
                        <StackPanel Spacing="6">
                            <TextBlock
                                FontSize="11"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Text="DCC ADDRESS" />
                            <NumberBox
                                HorizontalAlignment="Stretch"
                                LargeChange="10"
                                Maximum="9999"
                                Minimum="1"
                                SmallChange="1"
                                SpinButtonPlacementMode="Inline"
                                Value="{x:Bind ViewModel.LocoAddress, Mode=TwoWay}" />
                        </StackPanel>

                        <!--  Direction  -->
                        <StackPanel Spacing="6">
                            <TextBlock
                                FontSize="11"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Text="DIRECTION" />
                            <ToggleSwitch
                                IsOn="{x:Bind ViewModel.IsForward, Mode=TwoWay}"
                                OffContent="Backward"
                                        OnContent="Forward" />
                                </StackPanel>

                                <!--  Separator  -->
                                <Border
                                    Height="1"
                                    Margin="0,4"
                                    Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

                                <!--  Acceleration Ramp  -->
                                <StackPanel Spacing="6">
                                    <TextBlock
                                FontSize="11"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Text="ACCELERATION RAMP" />
                            <ToggleSwitch
                                IsOn="{x:Bind ViewModel.IsRampEnabled, Mode=TwoWay}"
                                OffContent="Instant"
                                OnContent="Gradual" />
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Spacing="4">
                                    <TextBlock
                                        FontSize="10"
                                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                        Text="Step Size" />
                                    <NumberBox
                                        IsEnabled="{x:Bind ViewModel.IsRampEnabled, Mode=OneWay}"
                                        Maximum="20"
                                        Minimum="1"
                                        SmallChange="1"
                                        SpinButtonPlacementMode="Compact"
                                        Value="{x:Bind ViewModel.RampStepSize, Mode=TwoWay}" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Spacing="4">
                                    <TextBlock
                                        FontSize="10"
                                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                        Text="Interval (ms)" />
                                    <NumberBox
                                        IsEnabled="{x:Bind ViewModel.IsRampEnabled, Mode=OneWay}"
                                        Maximum="500"
                                        Minimum="50"
                                        SmallChange="10"
                                        SpinButtonPlacementMode="Compact"
                                        Value="{x:Bind ViewModel.RampIntervalMs, Mode=TwoWay}" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>

                        <!--  Status  -->
                        <Border
                            Padding="12"
                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                            CornerRadius="6">
                            <StackPanel Spacing="2">
                                <TextBlock
                                    FontSize="10"
                                    FontWeight="SemiBold"
                                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                    Text="STATUS" />
                                <TextBlock
                                    FontSize="12"
                                    Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <!--  Ramping Indicator  -->
                        <Border
                            Padding="8"
                            Background="{ThemeResource SystemFillColorCautionBackgroundBrush}"
                            CornerRadius="4"
                            Visibility="{x:Bind ViewModel.IsRamping, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <ProgressRing
                                    Width="14"
                                    Height="14"
                                    IsActive="True" />
                                <TextBlock FontSize="11" Text="Ramping..." />
                            </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </Border>

                        <!--  CENTER: Speedometer  -->
                        <Border
                            Grid.Column="1"
                            Margin="0"
                            Padding="16"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!--  Speedometer  -->
                    <controls:SpeedometerControl
                        Grid.Row="0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        DisplayValue="{x:Bind ViewModel.ApproximateKmh, Mode=OneWay}"
                        MaxValue="126"
                        MinValue="0"
                        Value="{x:Bind ViewModel.Speed, Mode=OneWay}" />

                    <!--  Slider and Presets  -->
                    <StackPanel Grid.Row="1" Spacing="16">
                        <!--  Horizontal Speed Slider  -->
                        <StackPanel Spacing="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="0"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="0" />
                                <Slider
                                    Grid.Column="1"
                                    Margin="16,0"
                                    Maximum="126"
                                    Minimum="0"
                                    TickFrequency="10"
                                    TickPlacement="BottomRight"
                                    Value="{x:Bind ViewModel.Speed, Mode=TwoWay}" />
                                <TextBlock
                                    Grid.Column="2"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="126" />
                            </Grid>
                        </StackPanel>

                        <!--  Quick Presets  -->
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button
                                Grid.Column="0"
                                Height="40"
                                HorizontalAlignment="Stretch"
                                Background="#E81123"
                                Command="{x:Bind ViewModel.StopCommand}"
                                CornerRadius="6">
                                <TextBlock
                                    FontWeight="Bold"
                                    Foreground="White"
                                    Text="STOP" />
                            </Button>
                            <Button
                                Grid.Column="1"
                                Height="40"
                                HorizontalAlignment="Stretch"
                                Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                CommandParameter="20"
                                CornerRadius="6">
                                <TextBlock Text="20" />
                            </Button>
                            <Button
                                Grid.Column="2"
                                Height="40"
                                HorizontalAlignment="Stretch"
                                Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                CommandParameter="40"
                                CornerRadius="6">
                                <TextBlock Text="40" />
                            </Button>
                            <Button
                                Grid.Column="3"
                                Height="40"
                                HorizontalAlignment="Stretch"
                                Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                CommandParameter="60"
                                CornerRadius="6">
                                <TextBlock Text="60" />
                            </Button>
                            <Button
                                Grid.Column="4"
                                Height="40"
                                HorizontalAlignment="Stretch"
                                Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                CommandParameter="80"
                                CornerRadius="6">
                                <TextBlock Text="80" />
                            </Button>
                            <Button
                                Grid.Column="5"
                                Height="40"
                                HorizontalAlignment="Stretch"
                                Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                CommandParameter="126"
                                CornerRadius="6">
                                <TextBlock FontWeight="Bold" Text="MAX" />
                            </Button>
                        </Grid>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!--  FUNCTIONS PANEL: F0-F20 with accent colors (2 columns)  -->
                    <Border
                        Grid.Column="2"
                        Margin="8,0"
                        Padding="8"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                        <ScrollViewer Padding="0,0,4,0" VerticalScrollBarVisibility="Auto">
                            <StackPanel Spacing="4">
                                <TextBlock Margin="0,0,0,4" FontSize="12" FontWeight="SemiBold" Text="Functions" />
                                <Grid ColumnSpacing="4" RowSpacing="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <!--  F0: Light - Yellow with glow  -->
                                    <Grid Grid.Row="0" Grid.Column="0">
                                        <!-- Outer glow layer -->
                                        <Border Margin="-6" Background="#FFD700" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF0On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition>
                                                <ScalarTransition Duration="0:0:0.2" />
                                            </Border.OpacityTransition>
                                        </Border>
                                        <!-- Middle glow layer -->
                                        <Border Margin="-3" Background="#FFEA00" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF0On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <!-- Inner bright glow -->
                                        <Border Background="#FFFACD" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF0On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3DFFD700" Command="{x:Bind ViewModel.ToggleF0Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF0On, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="2"><FontIcon FontSize="11" Foreground="#FFD700" Glyph="&#xE793;" /><TextBlock FontSize="10" Text="F0" /></StackPanel>
                                        </ToggleButton>
                                    </Grid>
                                    <!--  F1: Sound - Blue with glow  -->
                                    <Grid Grid.Row="0" Grid.Column="1">
                                        <!-- Outer glow layer -->
                                        <Border Margin="-6" Background="#0078D4" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF1On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition>
                                                <ScalarTransition Duration="0:0:0.2" />
                                            </Border.OpacityTransition>
                                        </Border>
                                        <!-- Middle glow layer -->
                                        <Border Margin="-3" Background="#1E90FF" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF1On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <!-- Inner bright glow -->
                                        <Border Background="#87CEEB" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF1On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D0078D4" Command="{x:Bind ViewModel.ToggleF1Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF1On, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="2"><FontIcon FontSize="11" Foreground="#0078D4" Glyph="&#xE995;" /><TextBlock FontSize="10" Text="F1" /></StackPanel>
                                        </ToggleButton>
                                    </Grid>
                                    <!--  F2: Horn - Orange with glow  -->
                                    <Grid Grid.Row="1" Grid.Column="0">
                                        <!-- Outer glow layer -->
                                        <Border Margin="-6" Background="#FF8C00" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF2On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition>
                                                <ScalarTransition Duration="0:0:0.2" />
                                            </Border.OpacityTransition>
                                        </Border>
                                        <!-- Middle glow layer -->
                                        <Border Margin="-3" Background="#FFA500" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF2On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <!-- Inner bright glow -->
                                        <Border Background="#FFD580" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF2On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3DFF8C00" Command="{x:Bind ViewModel.ToggleF2Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF2On, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="2"><FontIcon FontSize="11" Foreground="#FF8C00" Glyph="&#xE7ED;" /><TextBlock FontSize="10" Text="F2" /></StackPanel>
                                        </ToggleButton>
                                    </Grid>
                                    <!--  F3: Door Close - Red with glow  -->
                                    <Grid Grid.Row="1" Grid.Column="1">
                                        <Border Margin="-6" Background="#E81123" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF3On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#FF4444" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF3On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#FF8888" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF3On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3DE81123" Command="{x:Bind ViewModel.ToggleF3Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF3On, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="2"><FontIcon FontSize="11" Foreground="#E81123" Glyph="&#xE7E8;" /><TextBlock FontSize="10" Text="F3" /></StackPanel>
                                        </ToggleButton>
                                    </Grid>
                                    <!--  F4: Door Release - Green with glow  -->
                                    <Grid Grid.Row="2" Grid.Column="0">
                                        <Border Margin="-6" Background="#107C10" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF4On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#32CD32" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF4On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#90EE90" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF4On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D107C10" Command="{x:Bind ViewModel.ToggleF4Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF4On, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="2"><FontIcon FontSize="11" Foreground="#107C10" Glyph="&#xE73E;" /><TextBlock FontSize="10" Text="F4" /></StackPanel>
                                        </ToggleButton>
                                    </Grid>
                                    <!--  F5: Shunting Light - Teal with glow  -->
                                    <Grid Grid.Row="2" Grid.Column="1">
                                        <Border Margin="-6" Background="#00B7C3" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF5On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#20C997" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF5On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#7FFFD4" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF5On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D00B7C3" Command="{x:Bind ViewModel.ToggleF5Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF5On, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="2"><FontIcon FontSize="11" Foreground="#00B7C3" Glyph="&#xE793;" /><TextBlock FontSize="10" Text="F5" /></StackPanel>
                                        </ToggleButton>
                                    </Grid>
                                    <!--  F6: Interior Light - Warm Yellow with glow  -->
                                    <Grid Grid.Row="3" Grid.Column="0">
                                        <Border Margin="-6" Background="#FFB900" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF6On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#FFD700" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF6On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#FFFACD" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF6On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3DFFB900" Command="{x:Bind ViewModel.ToggleF6Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF6On, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="2"><FontIcon FontSize="11" Foreground="#FFB900" Glyph="&#xE7E8;" /><TextBlock FontSize="10" Text="F6" /></StackPanel>
                                        </ToggleButton>
                                    </Grid>
                                    <!--  F7: Smoke - Gray with glow  -->
                                    <Grid Grid.Row="3" Grid.Column="1">
                                        <Border Margin="-6" Background="#767676" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF7On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#A0A0A0" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF7On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#C0C0C0" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF7On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D767676" Command="{x:Bind ViewModel.ToggleF7Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF7On, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="2"><FontIcon FontSize="11" Foreground="#767676" Glyph="&#xE753;" /><TextBlock FontSize="10" Text="F7" /></StackPanel>
                                        </ToggleButton>
                                    </Grid>
                                    <!--  F8: Pantograph - Magenta with glow  -->
                                    <Grid Grid.Row="4" Grid.Column="0">
                                        <Border Margin="-6" Background="#E3008C" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF8On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#FF69B4" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF8On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#FFB6C1" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF8On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3DE3008C" Command="{x:Bind ViewModel.ToggleF8Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF8On, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="2"><FontIcon FontSize="11" Foreground="#E3008C" Glyph="&#xE945;" /><TextBlock FontSize="10" Text="F8" /></StackPanel>
                                        </ToggleButton>
                                    </Grid>
                                    <!--  F9: Purple with glow  -->
                                    <Grid Grid.Row="4" Grid.Column="1">
                                        <Border Margin="-6" Background="#8764B8" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF9On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#9370DB" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF9On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#DDA0DD" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF9On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D8764B8" Command="{x:Bind ViewModel.ToggleF9Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF9On, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="2"><FontIcon FontSize="11" Foreground="#8764B8" Glyph="&#xE734;" /><TextBlock FontSize="10" Text="F9" /></StackPanel>
                                        </ToggleButton>
                                    </Grid>
                                    <!--  F10-F20: Accent colors with glow  -->
                                    <Grid Grid.Row="5" Grid.Column="0">
                                        <Border Margin="-6" Background="#00CC6A" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF10On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#32CD32" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF10On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#98FB98" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF10On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D00CC6A" Command="{x:Bind ViewModel.ToggleF10Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF10On, Mode=OneWay}">
                                            <TextBlock FontSize="10" Text="F10" />
                                        </ToggleButton>
                                    </Grid>
                                    <Grid Grid.Row="5" Grid.Column="1">
                                        <Border Margin="-6" Background="#038387" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF11On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#20B2AA" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF11On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#66CDAA" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF11On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D038387" Command="{x:Bind ViewModel.ToggleF11Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF11On, Mode=OneWay}">
                                            <TextBlock FontSize="10" Text="F11" />
                                        </ToggleButton>
                                    </Grid>
                                    <Grid Grid.Row="6" Grid.Column="0">
                                        <Border Margin="-6" Background="#C239B3" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF12On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#DA70D6" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF12On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#EE82EE" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF12On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3DC239B3" Command="{x:Bind ViewModel.ToggleF12Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF12On, Mode=OneWay}">
                                            <TextBlock FontSize="10" Text="F12" />
                                        </ToggleButton>
                                    </Grid>
                                    <Grid Grid.Row="6" Grid.Column="1">
                                        <Border Margin="-6" Background="#EA005E" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF13On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#FF1493" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF13On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#FF69B4" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF13On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3DEA005E" Command="{x:Bind ViewModel.ToggleF13Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF13On, Mode=OneWay}">
                                            <TextBlock FontSize="10" Text="F13" />
                                        </ToggleButton>
                                    </Grid>
                                    <Grid Grid.Row="7" Grid.Column="0">
                                        <Border Margin="-6" Background="#7A7574" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF14On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#A09999" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF14On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#C0C0C0" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF14On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D7A7574" Command="{x:Bind ViewModel.ToggleF14Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF14On, Mode=OneWay}">
                                            <TextBlock FontSize="10" Text="F14" />
                                        </ToggleButton>
                                    </Grid>
                                    <Grid Grid.Row="7" Grid.Column="1">
                                        <Border Margin="-6" Background="#567C73" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF15On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#6B8E8B" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF15On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#8FBC8F" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF15On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D567C73" Command="{x:Bind ViewModel.ToggleF15Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF15On, Mode=OneWay}">
                                            <TextBlock FontSize="10" Text="F15" />
                                        </ToggleButton>
                                    </Grid>
                                    <Grid Grid.Row="8" Grid.Column="0">
                                        <Border Margin="-6" Background="#8E562E" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF16On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#CD853F" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF16On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#DEB887" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF16On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D8E562E" Command="{x:Bind ViewModel.ToggleF16Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF16On, Mode=OneWay}">
                                            <TextBlock FontSize="10" Text="F16" />
                                        </ToggleButton>
                                    </Grid>
                                    <Grid Grid.Row="8" Grid.Column="1">
                                        <Border Margin="-6" Background="#847545" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF17On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#A08050" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF17On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#D2B48C" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF17On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D847545" Command="{x:Bind ViewModel.ToggleF17Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF17On, Mode=OneWay}">
                                            <TextBlock FontSize="10" Text="F17" />
                                        </ToggleButton>
                                    </Grid>
                                    <Grid Grid.Row="9" Grid.Column="0">
                                        <Border Margin="-6" Background="#525E54" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF18On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#708090" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF18On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#B0C4DE" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF18On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D525E54" Command="{x:Bind ViewModel.ToggleF18Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF18On, Mode=OneWay}">
                                            <TextBlock FontSize="10" Text="F18" />
                                        </ToggleButton>
                                    </Grid>
                                    <Grid Grid.Row="9" Grid.Column="1">
                                        <Border Margin="-6" Background="#4A5459" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF19On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                        </Border>
                                        <Border Margin="-3" Background="#696969" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF19On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <Border Background="#A9A9A9" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF19On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D4A5459" Command="{x:Bind ViewModel.ToggleF19Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF19On, Mode=OneWay}">
                                            <TextBlock FontSize="10" Text="F19" />
                                        </ToggleButton>
                                    </Grid>
                                                    <Grid Grid.Row="10" Grid.Column="0" Grid.ColumnSpan="2">
                                                        <Border Margin="-6" Background="#69797E" CornerRadius="10" Opacity="{x:Bind ViewModel.IsF20On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}">
                                                            <Border.OpacityTransition><ScalarTransition Duration="0:0:0.2" /></Border.OpacityTransition>
                                                        </Border>
                                                        <Border Margin="-3" Background="#778899" CornerRadius="7" Opacity="{x:Bind ViewModel.IsF20On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                                        <Border Background="#B0C4DE" CornerRadius="4" Opacity="{x:Bind ViewModel.IsF20On, Mode=OneWay, Converter={StaticResource GlowOpacityConverter}}" />
                                                        <ToggleButton Height="32" HorizontalAlignment="Stretch" Background="#3D69797E" Command="{x:Bind ViewModel.ToggleF20Command}" CornerRadius="4" IsChecked="{x:Bind ViewModel.IsF20On, Mode=OneWay}">
                                                            <TextBlock FontSize="10" Text="F20" />
                                                        </ToggleButton>
                                                    </Grid>
                                                </Grid>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </Border>

                                    <!--  RIGHT PANEL: Speed Categories  -->
                    <Border
                        Grid.Column="3"
                        Margin="8,0,0,0"
                        Padding="12,12,4,12"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <ScrollViewer Padding="0,0,12,0" VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="16">
                        <!--  Header  -->
                        <StackPanel Spacing="2">
                            <TextBlock
                                FontSize="16"
                                FontWeight="SemiBold"
                                Text="Speed Categories" />
                            <TextBlock
                                FontSize="11"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="German railway speed limits" />
                        </StackPanel>

                        <!--  Fernverkehr / ICE  -->
                        <Border
                            Padding="12"
                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                            CornerRadius="6">
                            <StackPanel Spacing="8">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon
                                        FontSize="14"
                                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                        Glyph="&#xE806;" />
                                    <TextBlock FontWeight="SemiBold" Text="Fernverkehr / ICE" />
                                </StackPanel>
                                <TextBlock
                                    FontSize="11"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="200-300 km/h" />
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <Button
                                        Width="42"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="100"
                                        Content="100"
                                        CornerRadius="4"
                                        FontSize="11" />
                                    <Button
                                        Width="42"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="115"
                                        Content="115"
                                        CornerRadius="4"
                                        FontSize="11" />
                                    <Button
                                        Width="42"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="126"
                                                        Content="MAX"
                                                        CornerRadius="4"
                                                        FontSize="10"
                                                        FontWeight="Bold" />
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>

                                        <!--  Hauptbahn  -->
                        <Border
                            Padding="12"
                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                            CornerRadius="6">
                            <StackPanel Spacing="8">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon
                                        FontSize="14"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Glyph="&#xE7C0;" />
                                    <TextBlock FontWeight="SemiBold" Text="Hauptbahn" />
                                </StackPanel>
                                <TextBlock
                                    FontSize="11"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="120-160 km/h" />
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <Button
                                        Width="44"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="75"
                                        Content="75"
                                        CornerRadius="4"
                                        FontSize="11" />
                                    <Button
                                        Width="44"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="90"
                                        Content="90"
                                        CornerRadius="4"
                                        FontSize="11" />
                                    <Button
                                        Width="44"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="100"
                                        Content="100"
                                        CornerRadius="4"
                                        FontSize="11" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!--  Nebenbahn  -->
                        <Border
                            Padding="12"
                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                            CornerRadius="6">
                            <StackPanel Spacing="8">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon
                                        FontSize="14"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Glyph="&#xE7C0;" />
                                    <TextBlock FontWeight="SemiBold" Text="Nebenbahn" />
                                </StackPanel>
                                <TextBlock
                                    FontSize="11"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="80-100 km/h" />
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <Button
                                        Width="44"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="50"
                                        Content="50"
                                        CornerRadius="4"
                                        FontSize="11" />
                                    <Button
                                        Width="44"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="55"
                                        Content="55"
                                        CornerRadius="4"
                                        FontSize="11" />
                                    <Button
                                        Width="44"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="60"
                                        Content="60"
                                        CornerRadius="4"
                                        FontSize="11" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!--  Bahnhof / Langsam  -->
                        <Border
                            Padding="12"
                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                            CornerRadius="6">
                            <StackPanel Spacing="8">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon
                                        FontSize="14"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Glyph="&#xE81A;" />
                                    <TextBlock FontWeight="SemiBold" Text="Bahnhof / Langsam" />
                                </StackPanel>
                                <TextBlock
                                    FontSize="11"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="40-60 km/h" />
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <Button
                                        Width="44"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="25"
                                        Content="25"
                                        CornerRadius="4"
                                        FontSize="11" />
                                    <Button
                                        Width="44"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="30"
                                        Content="30"
                                        CornerRadius="4"
                                        FontSize="11" />
                                    <Button
                                        Width="44"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="40"
                                        Content="40"
                                        CornerRadius="4"
                                        FontSize="11" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!--  Rangierfahrt  -->
                        <Border
                            Padding="12"
                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                            CornerRadius="6">
                            <StackPanel Spacing="8">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon
                                        FontSize="14"
                                        Foreground="{ThemeResource SystemFillColorCautionBrush}"
                                        Glyph="&#xE7C0;" />
                                    <TextBlock FontWeight="SemiBold" Text="Rangierfahrt" />
                                </StackPanel>
                                <TextBlock
                                    FontSize="11"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="25-40 km/h" />
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <Button
                                        Width="44"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="10"
                                        Content="10"
                                        CornerRadius="4"
                                        FontSize="11" />
                                    <Button
                                        Width="44"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="15"
                                        Content="15"
                                        CornerRadius="4"
                                        FontSize="11" />
                                    <Button
                                        Width="44"
                                        Height="32"
                                        Command="{x:Bind ViewModel.SetSpeedPresetCommand}"
                                        CommandParameter="25"
                                        Content="25"
                                        CornerRadius="4"
                                        FontSize="11" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>
