<UserControl
    x:Class="Moba.WinUI.Controls.LayoutDocument"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SurfaceBackgroundFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Tab Bar with Fluent Design  -->
        <Grid
            x:Name="TabBarGrid"
            Grid.Row="0"
            Height="40"
            Background="{ThemeResource SurfaceSecondaryBrush}"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="0,0,0,1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Tab Items Container  -->
            <ScrollViewer
                x:Name="TabScroll"
                Grid.Column="0"
                HorizontalScrollMode="Auto"
                VerticalScrollMode="Disabled">
                <ItemsControl
                    x:Name="TabItems"
                    Background="Transparent"
                    IsTabStop="False">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>

            <!--  New Tab Button  -->
            <Button
                x:Name="NewTabButton"
                Grid.Column="1"
                Width="40"
                Height="40"
                Margin="4,0"
                Padding="0"
                Background="Transparent"
                BorderBrush="Transparent"
                CornerRadius="4"
                ToolTipService.ToolTip="New Tab">
                <FontIcon
                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                    FontSize="16"
                    Glyph="&#xE710;" />
            </Button>
        </Grid>

        <!--  Content Area  -->
        <Grid
            x:Name="ContentGrid"
            Grid.Row="1"
            Background="{ThemeResource SurfaceBackgroundFillColorDefaultBrush}">
            <ContentPresenter x:Name="DocumentPresenter" />
        </Grid>
    </Grid>
</UserControl>
